# A shiny application about interval-censored and coarsened survival data.

The file `app.R` contains the code for the Shiny application.

The file `helpers.R` contains the code for generating the data for the graphs.

Try out the application at https://ugmi.shinyapps.io/shinythesis/.
